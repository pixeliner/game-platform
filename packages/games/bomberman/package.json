{
  "name": "@game-platform/game-bomberman",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @game-platform/engine build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @game-platform/engine build && tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@game-platform/engine": "workspace:*"
  }
}
