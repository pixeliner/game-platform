{
  "name": "@game-platform/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "pnpm --filter @game-platform/game-bomberman build && tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @game-platform/game-bomberman build && tsc --noEmit -p tsconfig.json",
    "lint": "eslint \"src/**/*.ts\"",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@game-platform/game-bomberman": "workspace:*",
    "pixi.js": "^8.9.1"
  }
}
