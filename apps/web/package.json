{
  "name": "@game-platform/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @game-platform/protocol build && pnpm --filter @game-platform/game-bomberman build && next dev",
    "build": "pnpm --filter @game-platform/protocol build && pnpm --filter @game-platform/game-bomberman build && next build",
    "start": "next start",
    "typecheck": "pnpm --filter @game-platform/protocol build && pnpm --filter @game-platform/game-bomberman build && next typegen && tsc --noEmit -p tsconfig.json",
    "lint": "eslint app src",
    "test": "vitest run --config vitest.config.ts"
  },
  "dependencies": {
    "@game-platform/game-bomberman": "workspace:*",
    "@game-platform/protocol": "workspace:*",
    "autoprefixer": "^10.4.20",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "next": "^15.1.6",
    "pixi.js": "^8.9.1",
    "postcss": "^8.5.1",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "tailwind-merge": "^2.6.0",
    "tailwindcss": "^3.4.17"
  },
  "devDependencies": {
    "@types/react": "^19.0.6",
    "@types/react-dom": "^19.0.3"
  }
}
